# Site

## Dev server
```bash
cd /Users/akashc/my-trankcopy/my-trank
npm run dev -- --host 127.0.0.1 --port 5174
```
Open: `http://127.0.0.1:5174/season`

## Build
```bash
npm run build
```

## Data lives here
- `public/data/*` is served directly in dev
- `dist/data/*` is used for production builds
